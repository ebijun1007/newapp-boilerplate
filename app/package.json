{
  "private": true,
  "name": "app",
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "clean": "turbo clean && rm -rf node_modules",
    "format": "biome format --write ./packages ./apps",
    "lint": "biome check --apply ./packages ./apps"
  },
  "devDependencies": {
    "@biomejs/biome": "1.4.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.11.0"
}
